Sparktech Laravel Auth v1.0.3

Changes:
- Installer now merges package imports and routes into the existing routes/api.php.
- Prevents duplicate <?php tags.
- Prevents duplicate use statements.
- Preserves the Laravel application routes.
- Package routes remain available under the configured auth prefix.
